# Archive

This directory is intentionally kept empty during normal development.

Historical private backups are stored outside the project directory so they are not mixed with source code, build outputs, or shared project files.
